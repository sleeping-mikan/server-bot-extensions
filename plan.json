{
  "$schema_note": "server-bot-v3 (https://github.com/sleeping-mikan/server-bot-v3) 向け拡張機能の実装計画。仕様は https://github.com/sleeping-mikan/server-bot-extensions を参照。",
  "target_bot": "server-bot-v3",
  "extension_dir_convention": "mikanassets/extension/<id>/commands.py",
  "generated_at": "2026-07-24",
  "operator_profile": "個人〜身内数人で運用するバニラMinecraft Java サーバー1台。管理者はDiscord上1名想定(.config の admin.members が1件)。この前提で「実際に月1回以上使われるか」を採用基準にしている。",
  "extension_api_summary": {
    "import_from": "core.state.ctx (モジュール先頭で読み込み、以後 tree/logger の変数に保持する)",
    "available": [
      "ctx.extension_commands_group (app_commands.Group, ロード時のみ有効)",
      "ctx.extension_logger (logging.Logger)",
      "bot.extensions.get_process() -> Popen | None",
      "bot.extensions.append_task(func) -> discord.ext.tasks ループを起動/停止管理付きで登録",
      "bot.extensions.write_server_in(command: str) -> (bool, str) サーバーstdinへ1行書き込み (allow_cmd等の許可リストは通らない、無条件書き込み)",
      "bot.utils.print_user / user_permission / not_enough_permission (bot本体と共通の権限system)",
      "ctx.server_path / ctx.server_name / ctx.backup_path / ctx.paths.base 等の読み取り専用パス情報",
      "bot.client.client (discord.Client) を直接importすればチャンネル取得/送信も可能"
    ],
    "known_limitations": [
      "サーバーの標準出力を拡張側で直接フックする仕組みは無い(公式ドキュメントで「フック機能は計画中」と明記)。そのためコマンド実行結果を読み取って加工する系の拡張は現状実装不可 — ただし rcon 拡張(下記)はRCONという別プロトコルを使うことでこの制約自体を回避している。",
      "write_server_in() は渡した文字列に改行を含めると複数コマンドとして注入されてしまう(ServerProcess.write が command + \"\\n\" をそのまま stdin に書くため)。ユーザー入力を渡す拡張は改行のサニタイズが必須。RCONはコマンド1個=1パケットなのでこの注入経路自体が存在しない。",
      "ctx.use_stop はサーバープロセス終了時に「意図的な停止か/クラッシュか」を区別する内部フラグに見えるが、クラッシュ検知時も stdout.py の読み取りスレッドが即座に True へ強制するため、外部から定期ポーリングする拡張機能ではこの2つを信頼して区別できない。'クラッシュ検知' を謳う拡張は誤検知(通常の/stop/ restartでも発火)するため実装しない。",
      "コアBotの /cmd serverin も実は結果を正確に取得できていない: bot/commands/cmd.py が ctx.is_back_discord=True にしてから最大3秒 ctx.cmd_logs をポーリングし、コマンド送信後に『次に来たstdoutの1行』を無条件にその結果として扱っている(server/stdout.py)。他のログ行の割り込みや複数行応答・タイムアウトに弱く、正確性は保証されない。RCONはコマンドと応答が同一TCP往復で1対1対応するため、この問題が原理的に起きない。"
    ]
  },
  "review_pass_2026-07-24": "1人目(ユーザー)がquick-commandsの冗長性を指摘、それを踏まえて2人目(共同開発者エージェント)に全アイデアの要否をレビューさせた。以下 extensions[] の status はそのレビュー後の最終結論。",
  "extensions": [
    {
      "id": "quick-commands",
      "name": "クイックコマンド",
      "status": "rejected",
      "category": "minecraft-vanilla-like",
      "summary": "weather / time / gamemode / difficulty / say を選択式スラッシュコマンド化する案。",
      "notes": "却下(既存)。コアBotの /cmd serverin (config の allow_mccmd に名前を足すだけで同じことができる) とターミナルチャンネルの生パススルーで完全に代替可能なため、拡張機能として作る意味がない。"
    },
    {
      "id": "auto-announce",
      "name": "定期アナウンス(季節メッセージ対応)",
      "status": "implemented",
      "category": "general",
      "compatible_servers": ["any"],
      "summary": "設定した間隔でサーバー内(write_server_in の say)とDiscordの両方に定型メッセージを自動送信する。各メッセージに任意で日付(MM-DD)を指定すると、その日付近辺だけ送信される(季節イベント告知を兼ねる)。",
      "features": [
        "append_task によるループ登録",
        "/extension-auto-announce list / add / remove でメッセージ管理 (add に date 引数で季節限定メッセージも登録可能)",
        "/extension-auto-announce config で間隔・送信先(サーバー/Discordチャンネル)を設定"
      ],
      "apis_used": ["append_task", "write_server_in", "extension_commands_group", "bot.client.client"],
      "notes": "review_pass_2026-07-24: 元々別案だった seasonal-announcer は独立拡張にするほどの差分が無いと判断し、date絞り込みオプションとしてこちらへ統合(MERGE)。"
    },
    {
      "id": "whitelist-ops-viewer",
      "name": "ホワイトリスト/OP一覧表示",
      "status": "implemented",
      "category": "minecraft-vanilla-like",
      "compatible_servers": ["vanilla", "paper", "spigot", "fabric", "forge"],
      "summary": "ctx.server_path 配下の whitelist.json / ops.json / banned-players.json を読み取り、埋め込みで一覧表示する。",
      "features": [
        "/extension-whitelist-ops-viewer whitelist",
        "/extension-whitelist-ops-viewer ops",
        "/extension-whitelist-ops-viewer bans"
      ],
      "apis_used": ["extension_commands_group", "ctx.server_path"],
      "notes": "review_pass_2026-07-24: KEEP。コアの /cmd stdin ls / send-discord はファイルの存在確認や転送はできてもJSONを整形表示しないため、実際のギャップを埋める。読み取り専用。"
    },
    {
      "id": "restart-warning-timer",
      "name": "再起動予告タイマー",
      "status": "implemented",
      "category": "minecraft-vanilla-like",
      "compatible_servers": ["vanilla", "paper", "spigot", "fabric", "forge"],
      "summary": "指定分数後を目標に、10分前/5分前/1分前/0分 と段階的にサーバー内へ警告broadcastする。実際の停止はコアBotの /restart や /server stop に委ねる。",
      "features": [
        "/extension-restart-warning-timer schedule <minutes>",
        "/extension-restart-warning-timer cancel"
      ],
      "apis_used": ["append_task", "write_server_in", "extension_commands_group"],
      "notes": "review_pass_2026-07-24: KEEP。コアの /restart は即時実行のみで予告機能が無いため、そこを補完する。"
    },
    {
      "id": "disk-space-watchdog",
      "name": "ディスク容量監視",
      "status": "implemented",
      "category": "general",
      "compatible_servers": ["any"],
      "summary": "定期的に shutil.disk_usage でサーバー設置先ドライブの空き容量をチェックし、閾値を下回ったらDiscordへ警告する(復帰したら再度警告可能な状態に戻す)。",
      "features": ["append_taskで定期チェック", "/extension-disk-space-watchdog status で現在値を即時表示", "/extension-disk-space-watchdog config で閾値・間隔・通知先チャンネルを設定"],
      "apis_used": ["append_task", "extension_commands_group", "extension_logger", "bot.client.client"],
      "notes": "review_pass_2026-07-24: KEEP。自宅サーバー運用でディスク枯渇は実際に起こる失敗モードで、コアBotに監視機能が無い。"
    },
    {
      "id": "backup-watch",
      "name": "バックアップ監視(未実施リマインド + 古いバックアップ自動整理)",
      "status": "implemented",
      "category": "general",
      "compatible_servers": ["any"],
      "summary": "旧案 backup-reminder と stale-backup-pruner を1つの拡張に統合。ctx.backup_path を直接走査するため手動記録が不要で、最終バックアップ経過時間の通知と、保持期間を超えた古いバックアップの自動削除の両方を提供する。",
      "features": [
        "/extension-backup-watch last — 最終バックアップからの経過時間を表示",
        "/extension-backup-watch prune-now — 保持期間を超えたバックアップを即時削除(要上位権限)",
        "/extension-backup-watch config — リマインド閾値・保持日数・自動削除有無・通知先チャンネルを設定",
        "append_task で定期チェックし、未実施が閾値を超えたらDiscord通知、auto_prune有効時は古いバックアップを自動削除"
      ],
      "apis_used": ["append_task", "extension_commands_group", "ctx.backup_path", "bot.client.client"],
      "notes": "review_pass_2026-07-24: 元々2案(backup-reminder: MERGE元, stale-backup-pruner: 共同開発者の新規提案)。ctx.backup_path のファイル名 (YYYY-MM-DD_HH_MM_SS-<name>) から日時を直接パースできるため『手動記録』は不要と判明し、完全自動化して統合した。"
    },
    {
      "id": "downtime-notifier",
      "name": "予期しないダウン通知",
      "status": "implemented",
      "category": "general",
      "compatible_servers": ["any"],
      "summary": "サーバープロセスが稼働中→停止に変わったことを検知し、一定の猶予時間(既定2分)経っても再起動されない場合にDiscordへ通知する。",
      "features": ["append_task で状態遷移を監視", "/extension-downtime-notifier config で猶予時間・通知先チャンネルを設定"],
      "apis_used": ["append_task", "extension_commands_group", "ctx.server_process", "bot.client.client"],
      "notes": "review_pass_2026-07-24: 共同開発者の提案『crash-restart-watchdog』を再設計。ctx.use_stop はクラッシュ時も stdout.py の読み取りスレッドが即座に True を強制するため、外部ポーリングでは『クラッシュ』と『意図的な停止』を区別できないと判明(known_limitationsに記載)。誤って/stop の度に「クラッシュ検知」と表示するのは有害なので、原因を断定しない中立的な『ダウン通知』に落とした。/restart はBotプロセスごと再起動され拡張タスクも道連れに再ロードされるため、このループが/restart自体を誤検知することはない。"
    },
    {
      "id": "idle-hours-notice",
      "name": "静かな時間帯の告知",
      "status": "cut",
      "category": "general",
      "notes": "review_pass_2026-07-24: 一旦実装したが、オーナー本人から『意味が分からないので不要』とフィードバックを受け撤去した(2026-07-24)。共同開発者エージェントが提案した3案の中でも一番弱いという注記付きだった案で、実際に使う側の判断としてはCUTが妥当。"
    },
    {
      "id": "rcon",
      "name": "RCON経由コマンド実行",
      "status": "implemented",
      "category": "minecraft-vanilla-like",
      "compatible_servers": ["vanilla", "paper", "spigot", "fabric", "forge (RCON対応サーバー全般)"],
      "summary": "Minecraft の RCON プロトコル(Source RCON)をstdlibのみで実装し、コマンドとその実行結果を確実に1対1で取得する。オーナー本人からのリクエストで追加。/cmd serverin の『次に来たstdout行を結果とみなす』不正確な仕組みと write_server_in の完全fire-and-forgetの両方を置き換える。",
      "features": [
        "/extension-rcon check — server.propertiesのRCON設定状況と疎通確認",
        "/extension-rcon cmd <command> — 任意コマンドを直接実行(要上位権限)",
        "/extension-rcon list / gamemode / weather / time / difficulty / say / tp / give / kill / xp / summon / setblock / title — 個別コマンドのタイプ付きエイリアス",
        "/extension-rcon effect give|clear",
        "/extension-rcon whitelist add|remove|list",
        "/extension-rcon player ban|pardon|kick|op|deop (要上位権限)"
      ],
      "permission_levels": {
        "storage": ".config の discord_commands.permission.commands_level (コアBotの他コマンドと完全に同じ辞書。ctx.text.command_permission はこの辞書そのもの)。キーが無ければ _KNOWN_PERMISSIONS のdefaultにフォールバックする(_perm()関数)。さらに拡張ロード時に不足キーをdefault値でメモリ上へ自動登録し、最初のコマンド実行時に1回だけ rewrite_config() で.configファイルへ書き戻す(core/config_loader.pyの起動時キー補完と同じ考え方)。既存の値は上書きしない。",
        "0 (既定)": ["rcon check", "rcon list", "rcon whitelist list"],
        "1 (既定)": ["rcon gamemode", "rcon weather", "rcon time", "rcon difficulty", "rcon say", "rcon tp", "rcon give", "rcon xp", "rcon summon", "rcon setblock", "rcon title", "rcon effect give", "rcon effect clear", "rcon whitelist add", "rcon whitelist remove"],
        "2 (既定)": ["rcon config", "rcon cmd(任意コマンド、allow_cmd等の許可リストを一切通らないため/cmd serverinのlevel1より高く設定)", "rcon kill", "rcon player ban/pardon/kick/op/deop"]
      },
      "apis_used": ["extension_commands_group", "ctx.server_path (server.propertiesの読み取り)", "asyncio/struct によるRCONクライアント自前実装(追加pip不要)", "bot.utils.user_permission + ctx.text.command_permission (コアBotと完全共通の権限レベル体系)"],
      "notes": "review_pass_2026-07-24: RCON接続情報(enable-rcon/rcon.port/rcon.password)は server.properties から読み取り、Discordにパスワードを入力させない設計にした。Bot=RCON接続元は同一マシン前提でhostは127.0.0.1固定。フェイクRCONサーバーを立てて認証成功/失敗・空応答・エラーメッセージ応答の4パターンをテスト済み。これによりtps-lag-reportがblockedだった根本原因(標準出力を読む手段が無い)は rcon cmd 経由で実質解消された(下記tps-lag-reportのnotes参照)。\n\nexecute チェイン専用のエイリアス(run/as/at/if-entity/custom)を一度追加したが、cmd がRCONへの生コマンド送信そのものであり `cmd command:\"execute as @a at @s run say hi\"` のように execute チェインをそのまま渡せば済むため、専用の execute グループは cmd と機能が完全に重複する薄いラッパーに過ぎないと判断し撤去した(2026-07-24)。\n\n(経緯メモ) execute run は任意チェインをRCONへそのまま渡すため機能的には100%再現していたが、chain全体を1個のスラッシュコマンドの型付き引数だけで表現しきることは理論上不可能(runの実行対象コマンドにexecute自身を指定できるため chain が無限に再帰しうる。例: `execute as X at Y as Z run execute at W run execute ... run <command>`、ユーザー指摘により判明)。ボタン/セレクト/モーダルで1修飾子ずつ何度でも追加でき再帰入れ子も組める execute builder を実装し、実際のdiscord.py 2.3.2をロードしたユニットテストで動作検証(nested実行結果が `execute as @a run execute at @s run say nested` と正しく組み上がること)まで完了したが、オーナー本人の『そこまでは要らない』という判断で撤去。続けて『だったらexecuteのエイリアスはいりません』というフィードバックを受け、run/as/at/if-entity/customを含むexecuteグループ全体を撤去した。\n\n(権限管理の変更) 当初は権限レベルを拡張独自の state.json + /extension-rcon config コマンドで管理していたが、オーナー本人から『.configに登録するべき』と指摘を受けた。調査の結果 main.py で ctx.text.command_permission = config[\"discord_commands\"][\"permission\"][\"commands_level\"] と代入されており、コアBotの全コマンドがこの1つの辞書をキー(例: \"terminal set\")で参照している既存パターンを発見。rconもこれに合わせ \"rcon cmd\" 等のキーで同じ辞書を読む _perm(key) 関数に置き換えた(デフォルト値は _KNOWN_PERMISSIONS 一箇所にまとめ、各コマンド呼び出し側に数値を重複させない)。.configに未設定なら _KNOWN_PERMISSIONS のdefaultにフォールバックする挙動を、実際のdiscord.py 2.3.2をロードしたユニットテストで検証済み(.configにキーがある場合はその値が優先され、無い場合のみdefaultが使われることを確認)。/extension-rcon config は権限レベル設定機能を削除し、timeout_secondsのみを扱う(権限変更コマンドが自分自身の権限レベルを変更できてしまう設計の危うさも同時に解消)。\n\n(追加: キー自動登録) 続けて『無ければキーを追加できますか』という質問を受け、core/config_loader.pyが起動時にコアBot自身のコマンドのcommands_levelキーを補完している既存パターン(INITIAL_COMMAND_PERMISSIONの未登録キーを.configへ書き足す処理)を参考に、rconも同様の自動登録を実装。拡張ロード時に不足キーをdefault値でctx.text.command_permissionへメモリ登録し、最初のrconコマンド実行時(_check_permission初回呼び出し時)に1回だけrewrite_config()で.configファイルへ永続化する。モジュールimport時点はモジュールレベルのため非同期コンテキストで無くrewrite_config()をawaitできないことが、初回コマンド実行時まで永続化を遅延させている理由。既存の値は上書きしないこと、rewrite_configが1回しか呼ばれないこと(2回目以降は何もしない)を実際のdiscord.py 2.3.2をロードしたユニットテストで検証済み。副次効果として、ctx.text.command_permissionはコアBotの/permission view detail:trueが列挙する辞書と同一なので、rconのキーもそこに一緒に表示されるようになった。\n\n(簡略化: 遅延永続化の撤去) 実機テストで拡張のロードが成功したのを確認した後、『普通に読み込まれた時点で同期関数で追記したらまずいんですか』と指摘を受けた。main.pyの起動シーケンスを再確認したところ、setup_commands()(bot.extensions.load()を呼ぶ関数)はasyncio.run(load_text())が既に完了・クローズした後、client.run()がイベントループを開始する前という完全に同期的なフェーズで実行されており、その時点で実行中のイベントループは存在しないと判明。rewrite_config()はasync def宣言だが中身は同期的なファイル書き込みでしかないため、asyncio.run(rewrite_config())をロード時点の_register_missing_permission_keys()内で直接呼べば安全と判断し、_permission_keys_pending_persistフラグと_persist_permission_keys_if_needed()(初回コマンド実行時まで永続化を遅延させる仕組み)を撤去。ロード完了と同時に即座に.configへ反映するよう簡略化した。イベントループが無い状態でのasyncio.run()呼び出しが成功すること、rewrite_configがロード直後(コマンド実行を待たず)に1回だけ呼ばれることを実際のdiscord.py 2.3.2をロードしたユニットテストで検証済み。"
    },
    {
      "id": "custom-whitelist-request",
      "name": "ホワイトリスト申請フロー",
      "status": "cut",
      "category": "minecraft-vanilla-like",
      "notes": "review_pass_2026-07-24: CUT。管理者1名の身内サーバーでは申請→承認のワークフローを作る手間に対して、管理者自身が /cmd serverin whitelist add を叩く方が速い。"
    },
    {
      "id": "uptime",
      "name": "稼働時間表示",
      "status": "cut",
      "category": "general",
      "notes": "review_pass_2026-07-24: CUT。拡張ロード時刻起点の値しか出せず実際のサーバー稼働時間ではないため誤解を招く上、/status で稼働状態自体はすでに見える。"
    },
    {
      "id": "dice-omikuji",
      "name": "サイコロ/おみくじ",
      "status": "cut",
      "category": "general-fun",
      "notes": "review_pass_2026-07-24: CUT。サーバー管理と無関係な娯楽コマンドで、実運用で継続的に使われる見込みが薄い。"
    },
    {
      "id": "safe-console-whitelist",
      "name": "安全コンソール(コマンド許可リスト方式)",
      "status": "cut",
      "category": "general",
      "notes": "review_pass_2026-07-24: CUT(冗長性を共同開発者エージェントが確認)。core/config_types.py の ServeInConfig.allow_cmd がまさに同じ機能(コマンド名の許可リスト)を提供済みで、config に名前を追加するだけで済む。拡張として再実装する理由がない。"
    },
    {
      "id": "tps-lag-report",
      "name": "TPS/ラグレポート",
      "status": "superseded_by_rcon",
      "category": "minecraft-vanilla-like",
      "notes": "review_pass_2026-07-24: extension_commands_group の標準API(write_server_in等)経由では引き続きblockedのままだが、rcon拡張の追加で実質的に解消。paper/spigot/forge系サーバーなら `/extension-rcon cmd tps` や `/extension-rcon cmd forge tps` でそのまま結果が返る。専用の整形コマンドは今のところ作らず、rconの cmd で十分としている。"
    },
    {
      "id": "cross-server-relay",
      "name": "複数サーバー間チャット中継",
      "status": "cut",
      "category": "advanced",
      "notes": "review_pass_2026-07-24: CUT。現在の運用はサーバー1台のみで、複数インスタンス運用というユースケース自体が発生していない。"
    }
  ]
}
